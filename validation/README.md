# Validation

This folder contains deterministic regression fixtures for pyqicharts v0.9.

The datasets and expected outputs are intentionally small. They are used by the test suite to check that core calculations remain stable across releases.

These files are not a substitute for an independently reviewed clinical/statistical validation pack, which remains a v1.0 readiness requirement.
